---
import type { CollectionEntry } from 'astro:content'

interface Props {
  prev?: CollectionEntry<'books'>
  next?: CollectionEntry<'books'>
  class?: string
}

const { prev, next, class: className } = Astro.props
---

<div class="grid grid-cols-2" class:list={[className]}>
  <div>
    {
      prev && (
        <a class="flex items-baseline gap-2 hover:text-accent" href={`/books/${prev.slug}`}>
          <i class="iconfont icon-arrow-left" />
          <span>{prev.slug.split('/')[1]}</span>
        </a>
      )
    }
  </div>
  <div>
    {
      next && (
        <a
          class="flex items-baseline justify-end gap-2 hover:text-accent"
          href={`/books/${next.slug}`}
        >
          <span>{next.slug.split('/')[1]}</span>
          <i class="iconfont icon-arrow-right" />
        </a>
      )
    }
  </div>
</div>
