# 允许这些依赖执行安装脚本（pnpm 默认会拦截构建脚本）
packages:
  - '.'

allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  sharp: true
  simple-git-hooks: true

# git hooks 里会调用 pnpm exec，关掉运行前的自动依赖校验，
# 否则 pnpm 会尝试重装 node_modules，在没有 TTY 的环境下直接中断提交
verifyDepsBeforeRun: false
